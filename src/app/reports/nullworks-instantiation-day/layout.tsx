import type { ReactNode } from "react";
import PhrononautPage from "./PhrononautPage";

export default function InstantiationDayLayout({ children: _children }: { children: ReactNode }) {
  return <PhrononautPage />;
}
