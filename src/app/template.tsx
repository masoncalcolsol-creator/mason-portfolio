import type { ReactNode } from "react";
import "./home-cinematic-global.css";

// Deployment trigger: repository reconnected to Vercel on 2026-07-03.
export default function Template({ children }: { children: ReactNode }) {
  return children;
}
